pipeline {
    agent {
        docker {
            image 'ruby'
        }
    }

    stages {
        stage('Build') {
            steps{
                sh 'cd api && bundle install'
            }
        }
        stage('Testing') {
            steps {
                echo 'cd api && rspec'
            }
        }
    }
}